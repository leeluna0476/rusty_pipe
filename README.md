# rusty_pipe
My rust practice.

## install rust (macOS, linux)
```sh
# install
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh

# check
rustc --version

# update
rustup update

# uninstall
rustup self uninstall
```
